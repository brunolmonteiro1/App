services:
  vosz-site:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vosz-site
    restart: unless-stopped
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
      PORT: "3000"
      HOSTNAME: "0.0.0.0"
    ports:
      - "3000:3000"
